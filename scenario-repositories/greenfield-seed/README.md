# Greenfield Seed

This repository intentionally contains build configuration and no application
source. A `GREENFIELD` engineering workflow must generate the application and
its acceptance tests inside an isolated workspace, then run `mvn clean test`.
