services:
  postgres:
    image: postgres:17-alpine
    container_name: agentic-software-engineer-postgres
    restart: unless-stopped

    environment:
      POSTGRES_DB: agentic_software_engineer
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5432:5432"

    volumes:
      - agentic_software_engineer_postgres_data:/var/lib/postgresql/data

    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U postgres -d agentic_software_engineer
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

volumes:
  agentic_software_engineer_postgres_data: